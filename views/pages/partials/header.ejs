<div class="header">
    <% console.log(user, JSON.stringify(doc, null, '\t')) %>
    <% if (!user) { %>
        <div class="header-link">
            <a href="/login">Login</a>
        </div>
    <% } else { %>
        <div class="header-username">
            Hello, <%= user.name %>
        </div>
        <div class="header-link">
            <a href="/logout">Logout</a>
        </div>
    <% } %>
</div>